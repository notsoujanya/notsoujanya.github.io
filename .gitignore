# Hugo build output — GitHub Actions builds this fresh on every deploy
public/

# Hugo's internal cache, generated automatically
resources/

# Hugo's lock file, auto-generated
.hugo_build.lock

# Mac system file that appears in every folder automatically
.DS_Store

# Your personal writing reference — stays on your computer only
WRITING_STYLE.md